{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn exam_system.wsgi --bind 0.0.0.0:$PORT --workers 4 --timeout 120 --access-logfile -",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
